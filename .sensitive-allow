# Paths that scripts/check_sensitive_files.py would flag but which are safe to
# share. One path per line, each with a comment saying who decided and why.
#
# This file exists so that overriding the check leaves a reviewable trace in the
# repository. A command-line flag would be typed once, forgotten, and the check
# would be dead. If this list grows without comments, the check is being
# worked around rather than used.
#
# (Empty: nothing in the repository currently needs an exception.)

# The check's own specification. It has to contain the things it detects: a
# fixture script with a private header, and a fake cluster path. Scoped to those
# two rules.
# Reviewed 2026-09-12.
tests/unit/test_sensitive_file_check.py:private-marker
tests/unit/test_sensitive_file_check.py:cluster-path
